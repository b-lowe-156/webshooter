
// @flow

const foo = (x: number): string => {
  if (x) {
     return "2"
  }
  return "default string"
}

interface articleEntity {
  name: string,
  nachname: string,
}

const fas = (x:articleEntity) => {
  
}